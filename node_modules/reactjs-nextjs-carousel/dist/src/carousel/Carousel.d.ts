interface ICarousel {
    slides: string[];
    autoSlide?: boolean;
    autoSlideInterval?: number;
    carouselWidth?: string;
    thumbWidth?: string;
    showThumbs?: boolean;
    showControlArrow?: boolean;
    showIndicators?: boolean;
    controllArrowSize?: number;
    indicatorSize?: string;
    effect?: "fade" | "slide";
}
declare const Carousel: ({ slides, autoSlide, autoSlideInterval, carouselWidth, thumbWidth, showThumbs, showControlArrow, showIndicators, controllArrowSize, indicatorSize, effect, }: ICarousel) => import("react/jsx-runtime").JSX.Element;
export default Carousel;
